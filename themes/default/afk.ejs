<!DOCTYPE html>
<html lang="en">
  <%- include('./components/head') %>
    <body>
      <!-- Mobile Toggle Button -->
      <button class="mobile-toggle" onclick="document.getElementById('sidebar').classList.toggle('active')">
          <i class="mdi mdi-menu"></i>
      </button>

      <div class="container-scroller">
        <!-- partial:partials/_sidebar.html -->
      <%- include('./components/sidebar') %>
        <!-- partial -->
        <div class="container-fluid page-body-wrapper">
          <!-- partial -->
        <div class="main-panel">
          <div class="content-wrapper">
           
           <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4">
                <div>
                    <h2 class="mb-1" style="font-weight: 700; letter-spacing: -0.025em; font-size: 1.8rem;">AFK Page</h2>
                    <p class="text-gray mb-0">Earn coins passively by keeping this page open.</p>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12 grid-margin stretch-card">
                    <div class="card" style="background: var(--card); border: 1px solid var(--border);">
                        <div class="card-body py-5">
                            <div style="background: rgba(59, 130, 246, 0.1); width: 80px; height: 80px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-bottom: 1.5rem;">
                                <i class="mdi mdi-clock-outline" style="font-size: 2.5rem; color: #3b82f6;"></i>
                            </div>
                            
                            <h4 class="mb-3" style="font-weight: 600;">Earning Status</h4>
                            
                            <p class="text-gray mb-4">
                                Every <span class="text-white font-weight-bold"><% if (settings.api.arcio["afk page"].every !== 1) { %><%= settings.api.arcio["afk page"].every %> seconds<% } else { %>second<% } %></span>, 
                                you will receive <span class="text-white font-weight-bold"><%= settings.api.arcio["afk page"].coins == 1 ? "a" : settings.api.arcio["afk page"].coins %> coin<%= settings.api.arcio["afk page"].coins == 1 ? "" : "s" %></span>.
                            </p>
                            
                            <div class="d-inline-flex align-items-center px-4 py-2 rounded-pill" style="background: rgba(16, 185, 129, 0.1); border: 1px solid rgba(16, 185, 129, 0.2);">
                                <span class="status-indicator mr-2" style="width: 8px; height: 8px; background: #10b981; border-radius: 50%; display: inline-block;"></span>
                                <span style="color: #10b981; font-weight: 500;">Websocket Connected</span>
                            </div>

                            <div class="mt-4 text-muted small">
                                <i class="mdi mdi-information-outline mr-1"></i> You must keep this tab open to earn coins.
                            </div>

                            <script>
                                <%- arcioafktext %>
                            </script>
                        </div>
                    </div>
                </div>
            </div>

           </div>
            <!-- content-wrapper ends -->
          <%- include('./components/footer') %>
          </div>
          <!-- main-panel ends -->
        </div>
        <!-- page-body-wrapper ends -->
      </div>
      <!-- container-scroller -->
      <%- include('./components/scripts') %>
    </body>
  </html>