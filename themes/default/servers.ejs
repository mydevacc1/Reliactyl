<!DOCTYPE html>
<html lang="en">
  <%- include('./components/head') %>
  <body>
    <!-- Mobile Toggle Button -->
    <button class="mobile-toggle" onclick="document.getElementById('sidebar').classList.toggle('active')">
        <i class="mdi mdi-menu"></i>
    </button>

    <div class="container-scroller">
      <%- include('./components/sidebar') %>
      <div class="container-fluid page-body-wrapper">
        <div class="main-panel">
          <div class="content-wrapper">
            
            <!-- Header -->
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4">
                <div>
                    <h2 class="mb-1" style="font-weight: 700; letter-spacing: -0.025em; font-size: 1.8rem;">Your Servers</h2>
                    <p class="text-gray mb-0">Manage and monitor your deployed server instances.</p>
                </div>
                <div class="mt-3 mt-md-0">
                    <a class="btn btn-primary d-flex align-items-center" href="/servers/new" style="padding: 0.75rem 1.5rem; font-weight: 600;">
                        <i class="mdi mdi-plus-circle-outline mr-2" style="font-size: 1.1rem;"></i> New Server
                    </a>
                </div>
            </div>

            <%- include('./components/alert') %>

            <% if (req.query.err && req.query.err !== "none") { %>
                <div class="alert fade show mb-4" role="alert" style="background: rgba(59, 130, 246, 0.1); border: 1px solid rgba(59, 130, 246, 0.2); color: #93c5fd; border-radius: var(--radius);">
                    <div class="d-flex align-items-center">
                        <i class="mdi mdi-information-outline mr-2" style="font-size: 1.25rem;"></i>
                        <span>
                            <% if (req.query.err == "CREATEDSERVER") { %>
                              Success! Your server has been created and is now installing.
                            <% } else if (req.query.err == "DELETEDSERVER") { %>
                              Success! Your server has been deleted.
                            <% } else if (req.query.err == "MODIFYSERVER") { %>
                              Success! Your server resources have been updated.
                            <% } %>
                        </span>
                    </div>
                </div>
            <% } %>

            <div class="row">
              <% if (pterodactyl.relationships.servers.data.length == 0) { %>
                <div class="col-12 text-center py-5">
                    <div style="background: var(--card); border: 1px solid var(--border); border-radius: var(--radius); padding: 4rem 2rem;">
                        <div style="background: var(--muted); width: 80px; height: 80px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem auto;">
                            <i class="mdi mdi-server-off" style="font-size: 2.5rem; color: var(--muted-foreground);"></i>
                        </div>
                        <h4 class="mb-2" style="font-weight: 600;">No Servers Found</h4>
                        <p class="text-gray mb-4" style="max-width: 400px; margin-left: auto; margin-right: auto;">You haven't deployed any servers yet. Get started by creating your first server instance.</p>
                        <a href="/servers/new" class="btn btn-primary btn-lg">Create Server</a>
                    </div>
                </div>
              <% } else { %>
                <% for (let i = 0, len = pterodactyl.relationships.servers.data.length; i < len; i++) { %>                
                <div class="col-md-6 col-lg-4 grid-margin stretch-card">
                  <div class="card h-100" style="transition: all 0.2s; border: 1px solid var(--border); background: var(--card);">
                    <div class="card-body d-flex flex-column">
                      <div class="d-flex justify-content-between align-items-start mb-3">
                          <div style="overflow: hidden;">
                              <h4 class="card-title mb-1 text-truncate" style="font-size: 1.1rem; font-weight: 600; color: var(--card-foreground);"><%= pterodactyl.relationships.servers.data[i].attributes.name %></h4>
                              <div class="d-flex align-items-center">
                                  <code style="background: var(--muted); padding: 2px 6px; border-radius: 4px; color: var(--muted-foreground); font-size: 0.75rem;"><%= pterodactyl.relationships.servers.data[i].attributes.identifier %></code>
                              </div>
                          </div>
                          <div class="badge" style="background: rgba(16, 185, 129, 0.1); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.2);">Active</div>
                      </div>

                      <div class="resource-stats mb-4 mt-2" style="background: var(--muted); border-radius: 8px; padding: 12px;">
                          <div class="d-flex justify-content-between mb-2 align-items-center">
                              <span class="text-gray" style="font-size: 0.85rem;"><i class="mdi mdi-memory mr-2"></i>CPU</span>
                              <span style="font-weight: 600; font-size: 0.9rem;"><%= pterodactyl.relationships.servers.data[i].attributes.limits.cpu/100 %> Core(s)</span>
                          </div>
                          <div class="d-flex justify-content-between mb-2 align-items-center">
                              <span class="text-gray" style="font-size: 0.85rem;"><i class="mdi mdi-chip mr-2"></i>RAM</span>
                              <span style="font-weight: 600; font-size: 0.9rem;"><%= pterodactyl.relationships.servers.data[i].attributes.limits.memory/1024 %> GB</span>
                          </div>
                          <div class="d-flex justify-content-between align-items-center">
                              <span class="text-gray" style="font-size: 0.85rem;"><i class="mdi mdi-harddisk mr-2"></i>Disk</span>
                              <span style="font-weight: 600; font-size: 0.9rem;"><%= pterodactyl.relationships.servers.data[i].attributes.limits.disk/1024 %> GB</span>
                          </div>
                      </div>

                      <div class="mt-auto">
                          <div class="d-flex gap-2" style="gap: 10px;">
                            <a href="<%= settings.pterodactyl.domain %>/server/<%= pterodactyl.relationships.servers.data[i].attributes.identifier %>" target="_blank" class="btn btn-primary flex-grow-1 d-flex align-items-center justify-content-center" style="padding: 0.6rem;">
                                Manage <i class="mdi mdi-open-in-new ml-2" style="font-size: 1rem;"></i>
                            </a>
                          </div>
                          <div class="d-flex gap-2 mt-2" style="gap: 10px;">
                            <a href="/servers/edit?id=<%= pterodactyl.relationships.servers.data[i].attributes.id %>" class="btn btn-outline-secondary flex-grow-1" style="padding: 0.5rem; border-color: var(--border); color: var(--muted-foreground);">Edit</a>
                            <% if (settings.renewals.status) { %> 
                              <a href="/renew?id=<%= pterodactyl.relationships.servers.data[i].attributes.id %>" class="btn btn-warning flex-grow-1" style="padding: 0.5rem; background-color: rgba(245, 158, 11, 0.1); border-color: rgba(245, 158, 11, 0.2); color: #fbbf24;">Renew</a>
                            <% } %>
                            <button onclick="confirmDelete('<%= pterodactyl.relationships.servers.data[i].attributes.id %>')" class="btn btn-outline-danger" style="padding: 0.5rem; border-color: rgba(239, 68, 68, 0.2); background: rgba(239, 68, 68, 0.05); color: #f87171;"><i class="mdi mdi-delete"></i></button>
                          </div>
                      </div>
                    </div>
                  </div>
                </div>
                <% } %>
              <% } %>
            </div>
          </div>
          <%- include('./components/footer') %>
        </div>
      </div>
    </div>
    <%- include('./components/scripts') %>
    
    <script>
        function confirmDelete(id) {
            if (confirm('Are you sure you want to delete this server? This action cannot be undone and all data will be lost.')) {
                window.location.href = '/delete?id=' + id;
            }
        }
    </script>

    <style>
        .card {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        .card:hover {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            transform: translateY(-2px);
        }
        .btn-outline-secondary:hover {
            background: var(--muted);
            color: var(--card-foreground);
            border-color: var(--muted);
        }
    </style>
  </body>
</html>