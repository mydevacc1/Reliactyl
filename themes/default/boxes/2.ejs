<style>
.action-card-inner {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 24px;
    height: 100%;
    min-height: 200px;
    display: flex;
    flex-direction: column;
}

.action-header {
    display: flex;
    align-items: flex-start;
    margin-bottom: 16px;
}

.action-icon {
    width: 48px;
    height: 48px;
    background: var(--primary);
    border: 1px solid var(--border);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 16px;
    color: var(--primary-foreground);
    flex-shrink: 0;
}

.action-icon i {
    font-size: 24px;
    line-height: 1;
    display: block;
}

.action-title {
    color: var(--card-foreground);
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 4px;
    line-height: 1.2;
}

.action-desc {
    color: var(--muted-foreground);
    font-size: 13px;
    line-height: 1.5;
}

.action-body {
    margin-bottom: 24px;
    flex: 1;
}

.action-text {
    color: var(--muted-foreground);
    font-size: 14px;
    line-height: 1.6;
    margin: 0;
}

.action-btn {
    margin-top: auto;
    width: 100%;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--primary);
    border: 1px solid var(--primary);
    color: var(--primary-foreground);
    border-radius: calc(var(--radius) - 2px);
    font-weight: 500;
    font-size: 14px;
    transition: all 0.2s;
    text-decoration: none;
}

.action-btn:hover {
    background: var(--card-foreground);
    color: var(--background);
    border-color: var(--card-foreground);
}
</style>

<div class="action-card-inner">
    <div class="action-header">
        <div class="action-icon">
            <i class="mdi mdi-plus"></i>
        </div>
        <div>
            <div class="action-title">Deploy Server</div>
            <div class="action-desc">Launch a new instance in seconds.</div>
        </div>
    </div>
    <div class="action-body">
         <p class="action-text">Start a new game server instantly. Choose from multiple locations and eggs to get started.</p>
    </div>
    <a href="../servers/new" class="action-btn">
        Deploy New Server
    </a>
</div>